import { Poppins } from "next/font/google";

export const title = Poppins({
  subsets: ["latin"],
  weight: ["600"],
});

export const description = Poppins({
  subsets: ["latin"],
  weight: ["400"],
})
